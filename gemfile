source 'https://rubygems.org'

gem 'jekyll', '~> 4.2'  # Use the latest compatible version of Jekyll

group :jekyll_plugins do
  gem 'jekyll-feed', '~> 0.15'  # Optional: for RSS feed
  gem 'jekyll-sitemap', '~> 1.4'  # Optional: for sitemap generation
end
